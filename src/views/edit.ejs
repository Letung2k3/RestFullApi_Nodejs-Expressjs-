<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Country</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="topnav">
        <a class="active" href="#home">Home</a>
        <a href="#news">News</a>
        <a href="#contact">Contact</a>
        <a href="#about">About</a>
    </div>

    <!-- Debugging line to verify data -->
    <!-- <pre><%= JSON.stringify(data, null, 2) %></pre> -->

    <div class="form-container">
        <h2>Update a Country</h2>
            <% if (data.length > 0) { %>
               <form action="/update/<%= data[0].ID %>" method="post">
                <div class="item" style="display:none ;">
                    <label for="id">ID</label>
                    <input type="number" id="id" name="id" value="<%= data[0].ID %>">
                </div>
                <div class="item">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" value="<%= data[0].Name %>">
                </div>
                <div class="item">
                    <label for="countryCode">Country Code</label>
                    <input type="text" id="countryCode" name="countryCode" value="<%= data[0].CountryCode %>">
                </div>
                <div class="item">
                    <label for="district">District</label>
                    <input type="text" id="district" name="district" value="<%= data[0].District %>">
                </div>
                <div class="item">
                    <label for="population">Population</label>
                    <input type="number" id="population" name="population" value="<%= data[0].Population %>">
                </div>

                <button type="submit">Save</button>
               </form>
            <% } else { %>
                <p>No data available</p>
            <% } %>
    </div>
</body>
</html>
